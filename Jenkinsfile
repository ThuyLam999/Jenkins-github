pipeline {
    agent any 
    stages {
        stage('Clone') {
            steps {
                git branch: 'main', url: 'https://github.com/ThuyLam999/Jenkins-github.git'
            }
        }
    }
}
